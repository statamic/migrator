collections:
  blog: '/blog/{year}/{month}/{day}/{slug}'
  things: '/things/{slug}'
taxonomies:
  tags: '/blog/tags/{slug}'
routes:
  /search: search
  /route-with-no-template:
    foo: bar
  /blog/feed:
    layout: feed
    template: feeds/blog
    content_type: atom
redirect:
  /articles: /
  /blog/posts: /blog
vanity:
  /products: /products-old
